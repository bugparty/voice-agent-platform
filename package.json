{
  "name": "voip-agent",
  "version": "0.1.0",
  "private": true,
  "description": "VoIP Agent with Twilio Media Streams, Silero VAD, and AI audio processing",
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "type-check": "pnpm -r type-check"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
